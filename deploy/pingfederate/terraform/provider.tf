provider "pingfederate" {
  # Prefer the provider's supported environment variables for credentials.
  #
  # Do not commit PingFederate administrator credentials or OAuth client
  # secrets to Terraform configuration/state.
  #
  # See README.md for the expected environment setup.
}
