apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: wai-pingfederate
  namespace: {{ .Values.namespace }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: wai-pingfederate
      app.kubernetes.io/instance: isolated-13-1
